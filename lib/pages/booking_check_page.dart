import 'package:flutter/material.dart';
class BookingCheckPage extends StatelessWidget{
  const BookingCheckPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Container(child: Text("data")));
  }
}
